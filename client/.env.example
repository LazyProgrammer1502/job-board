# Only needed for production build (Vercel)
# Point this to your Render backend URL — no trailing slash
VITE_API_URL=https://your-app-name.onrender.com

# Leave this empty in development — Vite proxy handles it
